# 服务合作管理系统 (SCMP) 接口契约设计与业务规则手册

> **文档版本**：v2.0.0 (全业务闭环深度研讨终版规格)  
> **面向对象**：系统架构师、前端/后端开发人员、外部系统对接实施人员、业务审计人员  
> **更新要点**：
> 1. 新增工单分批完工申请接口规范（串行审批强控制）；
> 2. 完工审核接口支持管理人员直接核减完工金额并录入扣款说明；
> 3. 服务商报销支持多批次勾选合并上传 1 张总发票；
> 4. 项目预算硬上限校验与在途金额调整通知规范。

---

## 一、 核心业务接口契约详细规范

### 1. 工单分批完工提报模块 (`/api/v1/vendor/work-orders`)

#### 1.1 工程师提报分批完工申请
* **URL**: `POST /api/v1/vendor/work-orders/{id}/submit-deliverable`
* **权限**: 仅限指派的履约工程师本人 (`ROLE_ENG`)
* **核心串行校验规则**：
  * 系统核验该工单是否存在处于 `PENDING` 审核中的交付批次；若存在，阻断并提示：`前一笔完工申请正在审核中，无法提报下一批完工`。
  * 校验：`applyAmount <= workOrder.amount - workOrder.completed_amount`。
* **请求体 (Request)**:
```json
{
  "progressPercent": 30.00, // 本期施工完成进度百分比 (如 30%)
  "applyAmount": 3000.00, // 本期申请完工结算金额 (元)
  "summary": "机房A区6台服务器机柜导轨已安装完毕，PDU双路引接就位，通电测试正常。",
  "attachmentUrls": [
    "/uploads/deliverables/rack_install_01.jpg",
    "/uploads/deliverables/power_test_report.pdf"
  ],
  "signSheetUrl": "/uploads/deliverables/customer_sign_sheet_phase1.jpg" // 业主现场签字单(必填)
}
```
* **系统处理**:
  * 生成 `work_order_deliverable` 批次记录，初始 `audit_status = 'PENDING'`，`settle_status = 'UNSETTLED'`。

#### 1.2 内部管理人员完工审核与核减扣款 (内部端)
* **URL**: `POST /api/v1/internal/work-orders/deliverables/{deliverableId}/audit`
* **权限**: 限内部审核管理人员 (`ROLE_AUDITOR`)
* **请求体 (Request)**:
```json
{
  "action": "APPROVE", // APPROVE(通过) 或 REJECT(驳回整改)
  "approvedAmount": 2800.00, // 实批金额 (若扣款可直接下调，例如申请3000核减为2800)
  "deductionAmount": 200.00, // 核减扣款金额
  "deductionReason": "现场施工超时2天，按合同违约条款扣罚200元",
  "comment": "设备安装规范，抽检合格，扣除工期超时款后准予验收通过"
}
```
* **系统处理逻辑**：
  * 若驳回：批次状态置为 `REJECTED`，工单回退整改，工程师必须补料重提，直至审批通过；
  * 若通过：
    1. 批次状态更新为 `APPROVED`；
    2. 工单累计已核准金额更新：`work_order.completed_amount += approvedAmount`；
    3. 核减差额（200元）释放回项目可用预算；
    4. **该批次核定的纯净实付金额 2,800 元进入服务商【待报销池】**；
    5. **系统自动向服务商接口人与工程师发送扣款明细通知**。

---

### 2. 服务商接口人报销归集与外部系统对接模块

#### 2.1 获取本公司所有已核准完工批次列表 (服务商端 - 接口负责人)
* **URL**: `GET /api/v1/vendor/settlements/candidate-deliverables`
* **权限**: 仅限服务商接口负责人 (`ROLE_VND_LIAISON`)
* **响应报文 (Response)**:
```json
{
  "code": 200,
  "data": [
    {
      "deliverableId": 101,
      "workOrderId": 1,
      "orderNo": "WO-20260901-001",
      "projectName": "某科技园区智慧机房改造工程",
      "title": "机房A区机柜安装与PDU引接 (第1批 30%)",
      "engineerName": "陈明",
      "approvedAmount": 2800.00, // 扣除200后的纯净可报销额
      "deductionAmount": 200.00,
      "deductionReason": "现场施工超时2天",
      "auditTime": "2026-09-24 14:00:00"
    },
    {
      "deliverableId": 102,
      "workOrderId": 2,
      "orderNo": "WO-20260901-002",
      "projectName": "市人民医院急诊大楼网络布线",
      "title": "急诊网络面板打底完工 (100% 全单完工)",
      "engineerName": "刘伟",
      "approvedAmount": 5000.00,
      "deductionAmount": 0.00,
      "deductionReason": null,
      "auditTime": "2026-09-24 15:30:00"
    }
  ]
}
```

#### 2.2 接口负责人合并多批次并上传 1 张总发票报销
* **URL**: `POST /api/v1/vendor/settlements/apply`
* **权限**: 限 `ROLE_VND_LIAISON`
* **请求体 (Request)**:
```json
{
  "deliverableIds": [101, 102], // 勾选多个完工核准批次
  "extraExpense": 0.00,
  "invoiceUrls": [
    "/uploads/invoices/inv_total_7800.pdf" // 允许开具 1 张 ¥7,800 元总发票！
  ],
  "bankSnapshot": {
    "bankName": "招商银行高新园支行",
    "bankAccount": "6214830129481234",
    "accountName": "深圳华创智能工程有限公司"
  }
}
```

#### 2.3 内部审核通过并自动推单至外部报销系统
* **URL**: `POST /api/v1/internal/settlements/{id}/audit-and-push`
* **权限**: 限 `ROLE_AUDITOR`
* **说明**: 内部管理人员确认发票金额（¥7,800）与申报总额完全匹配，点击通过，系统自动向外部财务系统发送报销单据。

#### 2.4 外部报销系统打款结果回调 (OpenAPI Inbound)
* **URL**: `POST /api/v1/openapi/reimbursement/callback`
* **Header**: `X-SCMP-Signature: {hmac_sha256}`
* **请求体 (Request)**:
```json
{
  "scmpSettleNo": "ST-20260924-001",
  "externalBillNo": "ERP-EXP-20260924-88910",
  "payStatus": "SUCCESS", // SUCCESS 或 FAILED
  "payAmount": 7800.00,
  "payTime": "2026-09-25 14:30:00",
  "bankSerialNo": "CMB202609250081294",
  "failReason": null
}
```
* **系统处理逻辑**：
  * `SUCCESS`：结算单变更为 `PAID`，挂接的完工批次全量变更为 `SETTLED`，短信通知服务商接口人到账；
  * `FAILED`：结算单变更为 `EXTERNAL_FAILED`，解冻关联完工批次为 `UNSETTLED`，通知服务商接口人重新提报。
